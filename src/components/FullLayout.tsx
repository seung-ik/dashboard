import React from 'react';

const FullLayout = () => {
  return (
    <div
      style={{
        background: 'gray',
        width: '100%',
        height: '100vh',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      error
    </div>
  );
};

export default FullLayout;
